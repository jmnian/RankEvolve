{"id": "2f5c3eb8-00c9-4c2f-b821-f03d69ef9c91", "code": "\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.5\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene IDF log(1 + (N-df+0.5)/(df+0.5)) (keeps IDF positive).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n", "language": "python", "parent_id": "25222a8f-4e16-445e-ba81-9205610092a7", "generation": 10, "timestamp": 1770187033.9102242, "iteration_found": 186, "metrics": {"bright_pony_ndcg@10": 0.0658861395850681, "bright_pony_recall@100": 0.22108834188752052, "bright_pony_index_time_ms": 7881.205212790519, "bright_pony_query_time_ms": 768.4093117713928, "beir_nfcorpus_ndcg@10": 0.3203101117259534, "beir_nfcorpus_recall@100": 0.2483152764947409, "beir_nfcorpus_index_time_ms": 13595.710739027709, "beir_nfcorpus_query_time_ms": 198.76771420240402, "beir_scifact_ndcg@10": 0.6792904076722974, "beir_scifact_recall@100": 0.9286666666666668, "beir_scifact_index_time_ms": 22969.41056707874, "beir_scifact_query_time_ms": 608.1762299872935, "beir_arguana_ndcg@10": 0.308529255945605, "beir_arguana_recall@100": 0.9371877230549608, "beir_arguana_index_time_ms": 23457.938095089048, "beir_arguana_query_time_ms": 31123.412041924894, "bright_theoremqa_theorems_ndcg@10": 0.024723883101193955, "bright_theoremqa_theorems_recall@100": 0.13157894736842105, "bright_theoremqa_theorems_index_time_ms": 54874.25879202783, "bright_theoremqa_theorems_query_time_ms": 1090.7313670031726, "beir_scidocs_ndcg@10": 0.15093446722492188, "beir_scidocs_recall@100": 0.34925, "beir_scidocs_index_time_ms": 93287.12861193344, "beir_scidocs_query_time_ms": 4123.699354939163, "bright_economics_ndcg@10": 0.15776239547531698, "bright_economics_recall@100": 0.4049818917453888, "bright_economics_index_time_ms": 57940.38881501183, "bright_economics_query_time_ms": 2747.3733723163605, "bright_biology_ndcg@10": 0.15754561005196502, "bright_biology_recall@100": 0.4008268430598528, "bright_biology_index_time_ms": 66502.96507496387, "bright_biology_query_time_ms": 2604.232009034604, "bright_earth_science_ndcg@10": 0.25551820954251026, "bright_earth_science_recall@100": 0.5549914739172905, "bright_earth_science_index_time_ms": 133110.9440010041, "bright_earth_science_query_time_ms": 4328.648534603417, "beir_fiqa_ndcg@10": 0.2409097924932401, "beir_fiqa_recall@100": 0.5431358636219747, "beir_fiqa_index_time_ms": 141968.39983621612, "beir_fiqa_query_time_ms": 6466.010526753962, "bright_stackoverflow_ndcg@10": 0.18452369099677834, "bright_stackoverflow_recall@100": 0.46510635088484537, "bright_stackoverflow_index_time_ms": 452578.1266889535, "bright_stackoverflow_query_time_ms": 9727.203901391476, "beir_trec-covid_ndcg@10": 0.6762617301090466, "beir_trec-covid_recall@100": 0.10987691635908037, "beir_trec-covid_index_time_ms": 589845.9160877392, "beir_trec-covid_query_time_ms": 1701.101461891085, "avg_ndcg@10": 0.26851630782699143, "avg_recall@100": 0.44125052458839514, "combined_score": 0.40670368123611444, "total_index_time_ms": 1658012.3925218359, "total_query_time_ms": 65487.765825819224, "total_time_ms": 1723500.158347655, "datasets_evaluated": 12, "datasets_failed": 0, "error": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 3 lines with 3 lines\nChange 2: Replace 4 lines with 4 lines", "parent_metrics": {"bright_pony_ndcg@10": 0.10817241570968407, "bright_pony_recall@100": 0.2943388796437178, "bright_pony_index_time_ms": 5246.453759260476, "bright_pony_query_time_ms": 718.1650018319488, "beir_nfcorpus_ndcg@10": 0.32103997694420416, "beir_nfcorpus_recall@100": 0.24724028451841487, "beir_nfcorpus_index_time_ms": 17034.95153505355, "beir_nfcorpus_query_time_ms": 195.04420598968863, "beir_scifact_ndcg@10": 0.6768448706423255, "beir_scifact_recall@100": 0.9286666666666668, "beir_scifact_index_time_ms": 17461.87464799732, "beir_scifact_query_time_ms": 612.385171931237, "beir_arguana_ndcg@10": 0.2722274376638082, "beir_arguana_recall@100": 0.9157744468236974, "beir_arguana_index_time_ms": 28073.3773522079, "beir_arguana_query_time_ms": 29423.290810082108, "bright_theoremqa_theorems_ndcg@10": 0.030958278260969384, "bright_theoremqa_theorems_recall@100": 0.1293859649122807, "bright_theoremqa_theorems_index_time_ms": 52342.084027826786, "bright_theoremqa_theorems_query_time_ms": 1013.8889509253204, "beir_scidocs_ndcg@10": 0.14276051501575812, "beir_scidocs_recall@100": 0.34103333333333335, "beir_scidocs_index_time_ms": 94759.17165074497, "beir_scidocs_query_time_ms": 4100.221581757069, "bright_biology_ndcg@10": 0.2769587703953891, "bright_biology_recall@100": 0.5409682720617826, "bright_biology_index_time_ms": 52434.636732097715, "bright_biology_query_time_ms": 2670.206071808934, "bright_economics_ndcg@10": 0.1546033827984985, "bright_economics_recall@100": 0.39627843991868733, "bright_economics_index_time_ms": 53940.81499520689, "bright_economics_query_time_ms": 2681.9661180488765, "bright_earth_science_ndcg@10": 0.3651269775296603, "bright_earth_science_recall@100": 0.6611948251637453, "bright_earth_science_index_time_ms": 105353.38702518493, "bright_earth_science_query_time_ms": 4220.1789310202, "beir_fiqa_ndcg@10": 0.21945753050637967, "beir_fiqa_recall@100": 0.5179668453974009, "beir_fiqa_index_time_ms": 136687.08191486076, "beir_fiqa_query_time_ms": 6582.878729328513, "bright_stackoverflow_ndcg@10": 0.18485706615900574, "bright_stackoverflow_recall@100": 0.465805767106117, "bright_stackoverflow_index_time_ms": 406842.5567909144, "bright_stackoverflow_query_time_ms": 9569.829924032092, "beir_trec-covid_ndcg@10": 0.6284489388293562, "beir_trec-covid_recall@100": 0.1052597258402444, "beir_trec-covid_index_time_ms": 588148.5706130043, "beir_trec-covid_query_time_ms": 1729.8924680799246, "avg_ndcg@10": 0.2817880133712532, "avg_recall@100": 0.4619927876155074, "combined_score": 0.4259518327666566, "total_index_time_ms": 1558324.96104436, "total_query_time_ms": 63517.94796483591, "total_time_ms": 1621842.9090091959, "datasets_evaluated": 12, "datasets_failed": 0, "error": 0.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are evolving a **constrained BM25** ranking function. We want **safe updates** only: tune hyperparameters, swap in known IDF/TF/length-norm/query-weight formulas, and combine these primitives meaningfully. Do **not** explore novel retrieval ideas \u2014 stick to BM25 and efficiently search the known space.\n\n## What you optimize\n\n- **combined_score** = 0.8 \u00d7 avg_recall@100 + 0.2 \u00d7 avg_ndcg@10 (higher is better).\n- Per-dataset: nDCG@10, Recall@100, index_time_ms, query_time_ms. Use these to target weak datasets.\n\n## What you can change (evolve)\n\n1. **Parameters** (k1, b, k3) \u2014 Tune within reasonable ranges: e.g. k1 in [0.5, 2.0], b in [0, 1], k3 in [0, 20].\n2. **idf(df, N)** \u2014 Swap for known formulas: Lucene (default), Robertson `log((N-df+0.5)/(df+0.5))`, ATIRE `log(N/df)`, BM25L `log((N+1)/(df+0.5))`, BM25+ `log((N+1)/df)`.\n3. **tf_saturated(tf, k1, norm)** \u2014 Swap for known formulas: Lucene `tf/(tf+k1*norm)` (default), Robertson `(k1+1)*tf/(tf+k1*norm)`, log-damped `log(1+tf)/(tf+k1*norm)`.\n4. **length_norm(doc_lengths, avgdl, b)** \u2014 Swap for known: pivoted `1-b+b*dl/avgdl` (default), none `1.0`, or other standard variants.\n5. **query_weights(query, k3, mode)** \u2014 Change mode or formula: \"unique\" (1 per term), \"count\" (qtf, default), \"saturated\" `(k3+1)*qtf/(k3+qtf)`.\n\nUse **SEARCH/REPLACE** diffs: SEARCH must exactly match the current code; REPLACE is your edit. Prefer **one component per edit** (e.g. only change k1/b, or only swap IDF formula).\n\n## What you must keep\n\n- **Interface**: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(query, top_k), BM25.score(query, doc_idx). Do not remove or rename these.\n- **BM25 structure**: Score = sum over query terms of (weight \u00d7 IDF \u00d7 saturated_TF). No new retrieval model.\n- **Numerical stability**: Avoid division by zero and NaNs (use a small epsilon where needed).\n\n## Guidelines\n\n- Prefer small, targeted changes (e.g. one hyperparameter or one formula swap).\n- Use known alternatives from the IR literature; do not invent new formulas.\n- This is effectively an efficient grid-search over a known search space \u2014 stay within it.\n", "user": "# Current Program Information\n- Fitness: 0.4260\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at 0.4260\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 3 lines with 3 lines\n- Metrics: bright_pony_ndcg@10: 0.1082, bright_pony_recall@100: 0.2943, bright_pony_index_time_ms: 7200.5478, bright_pony_query_time_ms: 735.0645, beir_nfcorpus_ndcg@10: 0.3210, beir_nfcorpus_recall@100: 0.2472, beir_nfcorpus_index_time_ms: 11325.2064, beir_nfcorpus_query_time_ms: 200.6728, beir_scifact_ndcg@10: 0.6768, beir_scifact_recall@100: 0.9287, beir_scifact_index_time_ms: 21218.7718, beir_scifact_query_time_ms: 601.9037, beir_arguana_ndcg@10: 0.2722, beir_arguana_recall@100: 0.9158, beir_arguana_index_time_ms: 29091.2031, beir_arguana_query_time_ms: 30327.8516, bright_theoremqa_theorems_ndcg@10: 0.0310, bright_theoremqa_theorems_recall@100: 0.1294, bright_theoremqa_theorems_index_time_ms: 55724.9186, bright_theoremqa_theorems_query_time_ms: 1060.2697, beir_scidocs_ndcg@10: 0.1428, beir_scidocs_recall@100: 0.3410, beir_scidocs_index_time_ms: 91599.9762, beir_scidocs_query_time_ms: 4353.5784, bright_biology_ndcg@10: 0.2770, bright_biology_recall@100: 0.5410, bright_biology_index_time_ms: 48959.7715, bright_biology_query_time_ms: 2470.6111, bright_economics_ndcg@10: 0.1546, bright_economics_recall@100: 0.3963, bright_economics_index_time_ms: 53534.8653, bright_economics_query_time_ms: 2865.5477, bright_earth_science_ndcg@10: 0.3651, bright_earth_science_recall@100: 0.6612, bright_earth_science_index_time_ms: 117087.6874, bright_earth_science_query_time_ms: 4286.6738, beir_fiqa_ndcg@10: 0.2195, beir_fiqa_recall@100: 0.5180, beir_fiqa_index_time_ms: 134571.9947, beir_fiqa_query_time_ms: 6332.4548, bright_stackoverflow_ndcg@10: 0.1849, bright_stackoverflow_recall@100: 0.4658, bright_stackoverflow_index_time_ms: 413053.7369, bright_stackoverflow_query_time_ms: 9492.8622, beir_trec-covid_ndcg@10: 0.6284, beir_trec-covid_recall@100: 0.1053, beir_trec-covid_index_time_ms: 567676.5226, beir_trec-covid_query_time_ms: 1730.9299, avg_ndcg@10: 0.2818, avg_recall@100: 0.4620, combined_score: 0.4260, total_index_time_ms: 1551045.2023, total_query_time_ms: 64458.4203, total_time_ms: 1615503.6226, datasets_evaluated: 12.0000, datasets_failed: 0.0000, error: 0.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace 3 lines with 3 lines\n- Metrics: beir_nfcorpus_ndcg@10: 0.3210, beir_nfcorpus_recall@100: 0.2472, beir_nfcorpus_index_time_ms: 8704.5544, beir_nfcorpus_query_time_ms: 190.3718, bright_pony_ndcg@10: 0.1082, bright_pony_recall@100: 0.2943, bright_pony_index_time_ms: 7138.6186, bright_pony_query_time_ms: 692.3652, beir_scifact_ndcg@10: 0.6768, beir_scifact_recall@100: 0.9287, beir_scifact_index_time_ms: 21527.8996, beir_scifact_query_time_ms: 583.6470, beir_arguana_ndcg@10: 0.2722, beir_arguana_recall@100: 0.9158, beir_arguana_index_time_ms: 22613.4872, beir_arguana_query_time_ms: 29945.3194, bright_theoremqa_theorems_ndcg@10: 0.0310, bright_theoremqa_theorems_recall@100: 0.1294, bright_theoremqa_theorems_index_time_ms: 58084.9240, bright_theoremqa_theorems_query_time_ms: 1123.6968, beir_scidocs_ndcg@10: 0.1428, beir_scidocs_recall@100: 0.3410, beir_scidocs_index_time_ms: 97102.1992, beir_scidocs_query_time_ms: 4102.7479, bright_economics_ndcg@10: 0.1546, bright_economics_recall@100: 0.3963, bright_economics_index_time_ms: 55942.3113, bright_economics_query_time_ms: 2963.7897, bright_biology_ndcg@10: 0.2770, bright_biology_recall@100: 0.5410, bright_biology_index_time_ms: 67990.4845, bright_biology_query_time_ms: 2580.1780, bright_earth_science_ndcg@10: 0.3651, bright_earth_science_recall@100: 0.6612, bright_earth_science_index_time_ms: 134079.8014, bright_earth_science_query_time_ms: 4471.5962, beir_fiqa_ndcg@10: 0.2195, beir_fiqa_recall@100: 0.5180, beir_fiqa_index_time_ms: 142096.7694, beir_fiqa_query_time_ms: 6574.1669, bright_stackoverflow_ndcg@10: 0.1849, bright_stackoverflow_recall@100: 0.4658, bright_stackoverflow_index_time_ms: 427667.6771, bright_stackoverflow_query_time_ms: 9640.2780, beir_trec-covid_ndcg@10: 0.6284, beir_trec-covid_recall@100: 0.1053, beir_trec-covid_index_time_ms: 584219.0210, beir_trec-covid_query_time_ms: 1734.2791, avg_ndcg@10: 0.2818, avg_recall@100: 0.4620, combined_score: 0.4260, total_index_time_ms: 1627167.7477, total_query_time_ms: 64602.4360, total_time_ms: 1691770.1837, datasets_evaluated: 12.0000, datasets_failed: 0.0000, error: 0.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace 3 lines with 3 lines\n- Metrics: bright_pony_ndcg@10: 0.1082, bright_pony_recall@100: 0.2943, bright_pony_index_time_ms: 6964.7459, bright_pony_query_time_ms: 692.0426, beir_nfcorpus_ndcg@10: 0.3210, beir_nfcorpus_recall@100: 0.2472, beir_nfcorpus_index_time_ms: 16499.8970, beir_nfcorpus_query_time_ms: 184.7101, beir_scifact_ndcg@10: 0.6768, beir_scifact_recall@100: 0.9287, beir_scifact_index_time_ms: 22534.7344, beir_scifact_query_time_ms: 599.9407, beir_arguana_ndcg@10: 0.2722, beir_arguana_recall@100: 0.9158, beir_arguana_index_time_ms: 27922.1981, beir_arguana_query_time_ms: 30313.7019, bright_theoremqa_theorems_ndcg@10: 0.0310, bright_theoremqa_theorems_recall@100: 0.1294, bright_theoremqa_theorems_index_time_ms: 51150.7952, bright_theoremqa_theorems_query_time_ms: 1080.0369, beir_scidocs_ndcg@10: 0.1428, beir_scidocs_recall@100: 0.3410, beir_scidocs_index_time_ms: 94249.7817, beir_scidocs_query_time_ms: 4139.9339, bright_economics_ndcg@10: 0.1546, bright_economics_recall@100: 0.3963, bright_economics_index_time_ms: 59147.9834, bright_economics_query_time_ms: 2795.7583, bright_biology_ndcg@10: 0.2770, bright_biology_recall@100: 0.5410, bright_biology_index_time_ms: 65658.7302, bright_biology_query_time_ms: 2636.0255, bright_earth_science_ndcg@10: 0.3651, bright_earth_science_recall@100: 0.6612, bright_earth_science_index_time_ms: 123516.0497, bright_earth_science_query_time_ms: 4379.8566, beir_fiqa_ndcg@10: 0.2195, beir_fiqa_recall@100: 0.5180, beir_fiqa_index_time_ms: 131532.2262, beir_fiqa_query_time_ms: 6375.6718, bright_stackoverflow_ndcg@10: 0.1849, bright_stackoverflow_recall@100: 0.4658, bright_stackoverflow_index_time_ms: 415697.4731, bright_stackoverflow_query_time_ms: 9845.3958, beir_trec-covid_ndcg@10: 0.6284, beir_trec-covid_recall@100: 0.1053, beir_trec-covid_index_time_ms: 575768.0605, beir_trec-covid_query_time_ms: 1709.9065, avg_ndcg@10: 0.2818, avg_recall@100: 0.4620, combined_score: 0.4260, total_index_time_ms: 1590642.6754, total_query_time_ms: 64752.9805, total_time_ms: 1655395.6559, datasets_evaluated: 12.0000, datasets_failed: 0.0000, error: 0.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.4260)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson/Sparck Jones BM25 IDF log((N-df+0.5)/(df+0.5)) (epsilon for stability).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Performs well on bright_pony_ndcg@10 (0.1082), Performs well on bright_pony_recall@100 (0.2943), Performs well on bright_pony_index_time_ms (6964.7459), Performs well on bright_pony_query_time_ms (692.0426), Performs well on beir_nfcorpus_ndcg@10 (0.3210), Performs well on beir_nfcorpus_recall@100 (0.2472), Performs well on beir_nfcorpus_index_time_ms (16499.8970), Performs well on beir_nfcorpus_query_time_ms (184.7101), Performs well on beir_scifact_ndcg@10 (0.6768), Performs well on beir_scifact_recall@100 (0.9287), Performs well on beir_scifact_index_time_ms (22534.7344), Performs well on beir_scifact_query_time_ms (599.9407), Performs well on beir_arguana_ndcg@10 (0.2722), Performs well on beir_arguana_recall@100 (0.9158), Performs well on beir_arguana_index_time_ms (27922.1981), Performs well on beir_arguana_query_time_ms (30313.7019), Performs well on bright_theoremqa_theorems_ndcg@10 (0.0310), Performs well on bright_theoremqa_theorems_recall@100 (0.1294), Performs well on bright_theoremqa_theorems_index_time_ms (51150.7952), Performs well on bright_theoremqa_theorems_query_time_ms (1080.0369), Performs well on beir_scidocs_ndcg@10 (0.1428), Performs well on beir_scidocs_recall@100 (0.3410), Performs well on beir_scidocs_index_time_ms (94249.7817), Performs well on beir_scidocs_query_time_ms (4139.9339), Performs well on bright_economics_ndcg@10 (0.1546), Performs well on bright_economics_recall@100 (0.3963), Performs well on bright_economics_index_time_ms (59147.9834), Performs well on bright_economics_query_time_ms (2795.7583), Performs well on bright_biology_ndcg@10 (0.2770), Performs well on bright_biology_recall@100 (0.5410), Performs well on bright_biology_index_time_ms (65658.7302), Performs well on bright_biology_query_time_ms (2636.0255), Performs well on bright_earth_science_ndcg@10 (0.3651), Performs well on bright_earth_science_recall@100 (0.6612), Performs well on bright_earth_science_index_time_ms (123516.0497), Performs well on bright_earth_science_query_time_ms (4379.8566), Performs well on beir_fiqa_ndcg@10 (0.2195), Performs well on beir_fiqa_recall@100 (0.5180), Performs well on beir_fiqa_index_time_ms (131532.2262), Performs well on beir_fiqa_query_time_ms (6375.6718), Performs well on bright_stackoverflow_ndcg@10 (0.1849), Performs well on bright_stackoverflow_recall@100 (0.4658), Performs well on bright_stackoverflow_index_time_ms (415697.4731), Performs well on bright_stackoverflow_query_time_ms (9845.3958), Performs well on beir_trec-covid_ndcg@10 (0.6284), Performs well on beir_trec-covid_recall@100 (0.1053), Performs well on beir_trec-covid_index_time_ms (575768.0605), Performs well on beir_trec-covid_query_time_ms (1709.9065), Performs well on avg_ndcg@10 (0.2818), Performs well on avg_recall@100 (0.4620), Performs well on combined_score (0.4260), Performs well on total_index_time_ms (1590642.6754), Performs well on total_query_time_ms (64752.9805), Performs well on total_time_ms (1655395.6559), Performs well on datasets_evaluated (12.0000), Performs well on datasets_failed (0.0000), Performs well on error (0.0000)\n\n### Program 2 (Score: 0.4260)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson/Sparck Jones BM25 IDF log((N-df+0.5)/(df+0.5)) (epsilon for stability).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Performs well on beir_nfcorpus_ndcg@10 (0.3210), Performs well on beir_nfcorpus_recall@100 (0.2472), Performs well on beir_nfcorpus_index_time_ms (8704.5544), Performs well on beir_nfcorpus_query_time_ms (190.3718), Performs well on bright_pony_ndcg@10 (0.1082), Performs well on bright_pony_recall@100 (0.2943), Performs well on bright_pony_index_time_ms (7138.6186), Performs well on bright_pony_query_time_ms (692.3652), Performs well on beir_scifact_ndcg@10 (0.6768), Performs well on beir_scifact_recall@100 (0.9287), Performs well on beir_scifact_index_time_ms (21527.8996), Performs well on beir_scifact_query_time_ms (583.6470), Performs well on beir_arguana_ndcg@10 (0.2722), Performs well on beir_arguana_recall@100 (0.9158), Performs well on beir_arguana_index_time_ms (22613.4872), Performs well on beir_arguana_query_time_ms (29945.3194), Performs well on bright_theoremqa_theorems_ndcg@10 (0.0310), Performs well on bright_theoremqa_theorems_recall@100 (0.1294), Performs well on bright_theoremqa_theorems_index_time_ms (58084.9240), Performs well on bright_theoremqa_theorems_query_time_ms (1123.6968), Performs well on beir_scidocs_ndcg@10 (0.1428), Performs well on beir_scidocs_recall@100 (0.3410), Performs well on beir_scidocs_index_time_ms (97102.1992), Performs well on beir_scidocs_query_time_ms (4102.7479), Performs well on bright_economics_ndcg@10 (0.1546), Performs well on bright_economics_recall@100 (0.3963), Performs well on bright_economics_index_time_ms (55942.3113), Performs well on bright_economics_query_time_ms (2963.7897), Performs well on bright_biology_ndcg@10 (0.2770), Performs well on bright_biology_recall@100 (0.5410), Performs well on bright_biology_index_time_ms (67990.4845), Performs well on bright_biology_query_time_ms (2580.1780), Performs well on bright_earth_science_ndcg@10 (0.3651), Performs well on bright_earth_science_recall@100 (0.6612), Performs well on bright_earth_science_index_time_ms (134079.8014), Performs well on bright_earth_science_query_time_ms (4471.5962), Performs well on beir_fiqa_ndcg@10 (0.2195), Performs well on beir_fiqa_recall@100 (0.5180), Performs well on beir_fiqa_index_time_ms (142096.7694), Performs well on beir_fiqa_query_time_ms (6574.1669), Performs well on bright_stackoverflow_ndcg@10 (0.1849), Performs well on bright_stackoverflow_recall@100 (0.4658), Performs well on bright_stackoverflow_index_time_ms (427667.6771), Performs well on bright_stackoverflow_query_time_ms (9640.2780), Performs well on beir_trec-covid_ndcg@10 (0.6284), Performs well on beir_trec-covid_recall@100 (0.1053), Performs well on beir_trec-covid_index_time_ms (584219.0210), Performs well on beir_trec-covid_query_time_ms (1734.2791), Performs well on avg_ndcg@10 (0.2818), Performs well on avg_recall@100 (0.4620), Performs well on combined_score (0.4260), Performs well on total_index_time_ms (1627167.7477), Performs well on total_query_time_ms (64602.4360), Performs well on total_time_ms (1691770.1837), Performs well on datasets_evaluated (12.0000), Performs well on datasets_failed (0.0000), Performs well on error (0.0000)\n\n### Program 3 (Score: 0.4260)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson/Sparck Jones BM25 IDF log((N-df+0.5)/(df+0.5)) (epsilon for stability).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Performs well on bright_pony_ndcg@10 (0.1082), Performs well on bright_pony_recall@100 (0.2943), Performs well on bright_pony_index_time_ms (7200.5478), Performs well on bright_pony_query_time_ms (735.0645), Performs well on beir_nfcorpus_ndcg@10 (0.3210), Performs well on beir_nfcorpus_recall@100 (0.2472), Performs well on beir_nfcorpus_index_time_ms (11325.2064), Performs well on beir_nfcorpus_query_time_ms (200.6728), Performs well on beir_scifact_ndcg@10 (0.6768), Performs well on beir_scifact_recall@100 (0.9287), Performs well on beir_scifact_index_time_ms (21218.7718), Performs well on beir_scifact_query_time_ms (601.9037), Performs well on beir_arguana_ndcg@10 (0.2722), Performs well on beir_arguana_recall@100 (0.9158), Performs well on beir_arguana_index_time_ms (29091.2031), Performs well on beir_arguana_query_time_ms (30327.8516), Performs well on bright_theoremqa_theorems_ndcg@10 (0.0310), Performs well on bright_theoremqa_theorems_recall@100 (0.1294), Performs well on bright_theoremqa_theorems_index_time_ms (55724.9186), Performs well on bright_theoremqa_theorems_query_time_ms (1060.2697), Performs well on beir_scidocs_ndcg@10 (0.1428), Performs well on beir_scidocs_recall@100 (0.3410), Performs well on beir_scidocs_index_time_ms (91599.9762), Performs well on beir_scidocs_query_time_ms (4353.5784), Performs well on bright_biology_ndcg@10 (0.2770), Performs well on bright_biology_recall@100 (0.5410), Performs well on bright_biology_index_time_ms (48959.7715), Performs well on bright_biology_query_time_ms (2470.6111), Performs well on bright_economics_ndcg@10 (0.1546), Performs well on bright_economics_recall@100 (0.3963), Performs well on bright_economics_index_time_ms (53534.8653), Performs well on bright_economics_query_time_ms (2865.5477), Performs well on bright_earth_science_ndcg@10 (0.3651), Performs well on bright_earth_science_recall@100 (0.6612), Performs well on bright_earth_science_index_time_ms (117087.6874), Performs well on bright_earth_science_query_time_ms (4286.6738), Performs well on beir_fiqa_ndcg@10 (0.2195), Performs well on beir_fiqa_recall@100 (0.5180), Performs well on beir_fiqa_index_time_ms (134571.9947), Performs well on beir_fiqa_query_time_ms (6332.4548), Performs well on bright_stackoverflow_ndcg@10 (0.1849), Performs well on bright_stackoverflow_recall@100 (0.4658), Performs well on bright_stackoverflow_index_time_ms (413053.7369), Performs well on bright_stackoverflow_query_time_ms (9492.8622), Performs well on beir_trec-covid_ndcg@10 (0.6284), Performs well on beir_trec-covid_recall@100 (0.1053), Performs well on beir_trec-covid_index_time_ms (567676.5226), Performs well on beir_trec-covid_query_time_ms (1730.9299), Performs well on avg_ndcg@10 (0.2818), Performs well on avg_recall@100 (0.4620), Performs well on combined_score (0.4260), Performs well on total_index_time_ms (1551045.2023), Performs well on total_query_time_ms (64458.4203), Performs well on total_time_ms (1615503.6226), Performs well on datasets_evaluated (12.0000), Performs well on datasets_failed (0.0000), Performs well on error (0.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.4260)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson BM25 IDF log((N-df+0.5)/(df+0.5)); alternatives: Lucene log(1+(N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Alternative approach to bright_pony_ndcg@10, Alternative approach to bright_pony_recall@100\n\n### Program D2 (Score: 0.4260)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson IDF log((N-df+0.5)/(df+0.5)).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Alternative approach to bright_pony_ndcg@10, Alternative approach to bright_pony_recall@100\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.4260, Type: Experimental)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson/Sparck Jones BM25 IDF log((N-df+0.5)/(df+0.5)) (epsilon for stability).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.3992, Type: Exploratory)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 1.1\n    b: float = 0.6\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene IDF log(1 + (N-df+0.5)/(df+0.5)) (keeps IDF positive).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.4147, Type: Experimental)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson/Sparck Jones BM25 IDF log((N-df+0.5)/(df+0.5)) (epsilon for stability).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Two safe, targeted BM25 moves that often lift nDCG@10 without hurting Recall@100 too much:\n\n1) **Use Lucene\u2019s positive IDF** (`log(1 + (N-df+0.5)/(df+0.5))`) instead of raw Robertson IDF. Your current code **skips negative IDF terms** (`if idf_val <= 0: continue`), which can remove useful common terms entirely; Lucene IDF keeps them small-but-positive, typically helping ranking quality on noisy/webby corpora.\n\n2) **Tune b upward** to add more length normalization. Current `b=0.2` is quite low; many BEIR-like corpora benefit from more normalization. Keep k1 the same for a minimal step.\n\n### Patch 1: swap IDF to Lucene\n<<<<<<< SEARCH\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Robertson/Sparck Jones BM25 IDF log((N-df+0.5)/(df+0.5)) (epsilon for stability).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n=======\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene IDF log(1 + (N-df+0.5)/(df+0.5)) (keeps IDF positive).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n>>>>>>> REPLACE\n\n### Patch 2: adjust b (length normalization)\n<<<<<<< SEARCH\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.2\n    k3: float = 8.0\n=======\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.5\n    k3: float = 8.0\n>>>>>>> REPLACE\n\n**Why these are \u201csafe\u201d**: they stay strictly within standard BM25 primitives (known Lucene IDF + modest hyperparameter tuning), keep numerical stability, and don\u2019t change interfaces or add any new retrieval logic."]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}