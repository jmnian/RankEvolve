{"id": "fd989be8-1c12-40ac-8586-8aaedd46b51b", "code": "\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.55\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n", "language": "python", "parent_id": "468c4470-57d2-457f-bce6-69a8fad131da", "generation": 5, "timestamp": 1770024916.714389, "iteration_found": 41, "metrics": {"beir_nfcorpus_ndcg@10": 0.3196872632414022, "beir_nfcorpus_recall@100": 0.25433136398646605, "beir_nfcorpus_index_time_ms": 3474.90133397514, "beir_nfcorpus_query_time_ms": 90.79441602807492, "beir_scifact_ndcg@10": 0.6795216202580148, "beir_scifact_recall@100": 0.9279999999999999, "beir_scifact_index_time_ms": 4702.690458972938, "beir_scifact_query_time_ms": 321.50629098759964, "bright_pony_ndcg@10": 0.06225928519327497, "bright_pony_recall@100": 0.20622188573990632, "bright_pony_index_time_ms": 1315.4400830389932, "bright_pony_query_time_ms": 412.1694999630563, "beir_arguana_ndcg@10": 0.31650827526393605, "beir_arguana_recall@100": 0.9400428265524625, "beir_arguana_index_time_ms": 5666.454833000898, "beir_arguana_query_time_ms": 17549.80991699267, "bright_theoremqa_theorems_ndcg@10": 0.02507054766589453, "bright_theoremqa_theorems_recall@100": 0.13157894736842105, "bright_theoremqa_theorems_index_time_ms": 10529.528165992815, "bright_theoremqa_theorems_query_time_ms": 737.968000001274, "beir_scidocs_ndcg@10": 0.15129381116121096, "beir_scidocs_recall@100": 0.35050000000000003, "beir_scidocs_index_time_ms": 19106.26916703768, "beir_scidocs_query_time_ms": 3035.0098749622703, "bright_economics_ndcg@10": 0.15412082782144937, "bright_economics_recall@100": 0.4035438017200645, "bright_economics_index_time_ms": 13602.380375028588, "bright_economics_query_time_ms": 1979.7337910276838, "bright_biology_ndcg@10": 0.14033960094965606, "bright_biology_recall@100": 0.3884295316334151, "bright_biology_index_time_ms": 14509.90374997491, "bright_biology_query_time_ms": 1926.604792010039, "beir_fiqa_ndcg@10": 0.24319419825017602, "beir_fiqa_recall@100": 0.5439393125735718, "beir_fiqa_index_time_ms": 32797.76095895795, "beir_fiqa_query_time_ms": 5344.4642089889385, "bright_earth_science_ndcg@10": 0.22817254576962936, "bright_earth_science_recall@100": 0.5462557337046902, "bright_earth_science_index_time_ms": 30007.91029201355, "bright_earth_science_query_time_ms": 3225.0194160151295, "bright_stackoverflow_ndcg@10": 0.18360262867531263, "bright_stackoverflow_recall@100": 0.4649614863332011, "bright_stackoverflow_index_time_ms": 97101.44995898008, "bright_stackoverflow_query_time_ms": 7021.102749975398, "beir_trec-covid_ndcg@10": 0.6719062974045843, "beir_trec-covid_recall@100": 0.10963276794224942, "beir_trec-covid_index_time_ms": 130539.44662498543, "beir_trec-covid_query_time_ms": 1292.3241659882478, "avg_ndcg@10": 0.26463974180454514, "avg_recall@100": 0.43895313812953735, "combined_score": 0.4040904588645389, "total_index_time_ms": 363354.136001959, "total_query_time_ms": 42936.507122940384, "total_time_ms": 406290.64312489936, "datasets_evaluated": 12, "datasets_failed": 0, "error": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n      \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df),...\n      return np.log((N + 1e-9) / (df + 1e-9))\nwith:\n  def idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n      \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df),...\n      return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\nChange 2: Replace:\n  class Parameters:\n      k1: float = 0.9\n      b: float = 0.4\n      k3: float = 8.0\nwith:\n  class Parameters:\n      k1: float = 0.9\n      b: float = 0.55\n      k3: float = 8.0", "parent_metrics": {"beir_nfcorpus_ndcg@10": 0.32006437959136086, "beir_nfcorpus_recall@100": 0.2549442033620135, "beir_nfcorpus_index_time_ms": 3530.139375012368, "beir_nfcorpus_query_time_ms": 91.29150002263486, "beir_scifact_ndcg@10": 0.6780212636624363, "beir_scifact_recall@100": 0.9286666666666668, "beir_scifact_index_time_ms": 4705.329125048593, "beir_scifact_query_time_ms": 322.0967500237748, "bright_pony_ndcg@10": 0.07748451273448467, "bright_pony_recall@100": 0.25029186530902775, "bright_pony_index_time_ms": 1312.6000419724733, "bright_pony_query_time_ms": 409.3517499859445, "beir_arguana_ndcg@10": 0.2952142469067258, "beir_arguana_recall@100": 0.926481084939329, "beir_arguana_index_time_ms": 5750.314333010465, "beir_arguana_query_time_ms": 17377.562750014476, "bright_theoremqa_theorems_ndcg@10": 0.023780806596489876, "bright_theoremqa_theorems_recall@100": 0.1337719298245614, "bright_theoremqa_theorems_index_time_ms": 10513.099375006277, "bright_theoremqa_theorems_query_time_ms": 745.3974170493893, "beir_scidocs_ndcg@10": 0.14992968837634146, "beir_scidocs_recall@100": 0.3478333333333334, "beir_scidocs_index_time_ms": 19144.02758301003, "beir_scidocs_query_time_ms": 2984.2455420293845, "bright_biology_ndcg@10": 0.18374451061205666, "bright_biology_recall@100": 0.43204831845608543, "bright_biology_index_time_ms": 14462.696583999787, "bright_biology_query_time_ms": 1948.9368330105208, "bright_economics_ndcg@10": 0.16923988487221636, "bright_economics_recall@100": 0.4016863347391702, "bright_economics_index_time_ms": 13651.67220798321, "bright_economics_query_time_ms": 1985.6074579874985, "beir_fiqa_ndcg@10": 0.23667710486948956, "beir_fiqa_recall@100": 0.5372055285249729, "beir_fiqa_index_time_ms": 32453.11841601506, "beir_fiqa_query_time_ms": 5475.880332989618, "bright_earth_science_ndcg@10": 0.30939825600326987, "bright_earth_science_recall@100": 0.6112948089921775, "bright_earth_science_index_time_ms": 31261.482166009955, "bright_earth_science_query_time_ms": 3366.4837909745984, "bright_stackoverflow_ndcg@10": 0.18447164382299205, "bright_stackoverflow_recall@100": 0.469874789493262, "bright_stackoverflow_index_time_ms": 96603.06420800043, "bright_stackoverflow_query_time_ms": 7016.885459015612, "beir_trec-covid_ndcg@10": 0.6690793014424301, "beir_trec-covid_recall@100": 0.10892541820470526, "beir_trec-covid_index_time_ms": 129549.72495802213, "beir_trec-covid_query_time_ms": 1254.4965000124648, "avg_ndcg@10": 0.27475879995752445, "avg_recall@100": 0.4502520234871088, "combined_score": 0.41515337878119196, "total_index_time_ms": 362937.2683730908, "total_query_time_ms": 42978.23608311592, "total_time_ms": 405915.5044562067, "datasets_evaluated": 12, "datasets_failed": 0, "error": 0.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are evolving a **constrained BM25** ranking function. We want **safe updates** only: tune hyperparameters, swap in known IDF/TF/length-norm/query-weight formulas, and combine these primitives meaningfully. Do **not** explore novel retrieval ideas \u2014 stick to BM25 and efficiently search the known space.\n\n## What you optimize\n\n- **combined_score** = 0.8 \u00d7 avg_recall@100 + 0.2 \u00d7 avg_ndcg@10 (higher is better).\n- Per-dataset: nDCG@10, Recall@100, index_time_ms, query_time_ms. Use these to target weak datasets.\n\n## What you can change (evolve)\n\n1. **Parameters** (k1, b, k3) \u2014 Tune within reasonable ranges: e.g. k1 in [0.5, 2.0], b in [0, 1], k3 in [0, 20].\n2. **idf(df, N)** \u2014 Swap for known formulas: Lucene (default), Robertson `log((N-df+0.5)/(df+0.5))`, ATIRE `log(N/df)`, BM25L `log((N+1)/(df+0.5))`, BM25+ `log((N+1)/df)`.\n3. **tf_saturated(tf, k1, norm)** \u2014 Swap for known formulas: Lucene `tf/(tf+k1*norm)` (default), Robertson `(k1+1)*tf/(tf+k1*norm)`, log-damped `log(1+tf)/(tf+k1*norm)`.\n4. **length_norm(doc_lengths, avgdl, b)** \u2014 Swap for known: pivoted `1-b+b*dl/avgdl` (default), none `1.0`, or other standard variants.\n5. **query_weights(query, k3, mode)** \u2014 Change mode or formula: \"unique\" (1 per term), \"count\" (qtf, default), \"saturated\" `(k3+1)*qtf/(k3+qtf)`.\n\nUse **SEARCH/REPLACE** diffs: SEARCH must exactly match the current code; REPLACE is your edit. Prefer **one component per edit** (e.g. only change k1/b, or only swap IDF formula).\n\n## What you must keep\n\n- **Interface**: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(query, top_k), BM25.score(query, doc_idx). Do not remove or rename these.\n- **BM25 structure**: Score = sum over query terms of (weight \u00d7 IDF \u00d7 saturated_TF). No new retrieval model.\n- **Numerical stability**: Avoid division by zero and NaNs (use a small epsilon where needed).\n\n## Guidelines\n\n- Prefer small, targeted changes (e.g. one hyperparameter or one formula swap).\n- Use known alternatives from the IR literature; do not invent new formulas.\n- This is effectively an efficient grid-search over a known search space \u2014 stay within it.\n", "user": "# Current Program Information\n- Fitness: 0.4152\n- Feature coordinates: \n- Focus areas: - Fitness improved: 0.4151 \u2192 0.4152\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  def idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n      \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df),...\n      return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\nwith:\n  def idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n      \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df),...\n      return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\nChange 2: Replace:\n  class Parameters:\n      k1: float = 1.2\n      b: float = 0.6\n      k3: float = 8.0\nwith:\n  class Parameters:\n      k1: float = 0.9\n      b: float = 0.4\n      k3: float = 8.0\n- Metrics: beir_nfcorpus_ndcg@10: 0.3201, beir_nfcorpus_recall@100: 0.2551, beir_nfcorpus_index_time_ms: 3477.4060, beir_nfcorpus_query_time_ms: 89.8102, beir_scifact_ndcg@10: 0.6766, beir_scifact_recall@100: 0.9287, beir_scifact_index_time_ms: 4665.6751, beir_scifact_query_time_ms: 321.7332, beir_arguana_ndcg@10: 0.2951, beir_arguana_recall@100: 0.9265, beir_arguana_index_time_ms: 5682.0987, beir_arguana_query_time_ms: 17352.5388, bright_pony_ndcg@10: 0.0777, bright_pony_recall@100: 0.2497, bright_pony_index_time_ms: 1322.8384, bright_pony_query_time_ms: 406.5820, beir_scidocs_ndcg@10: 0.1497, beir_scidocs_recall@100: 0.3478, beir_scidocs_index_time_ms: 18716.5670, beir_scidocs_query_time_ms: 3000.2234, bright_theoremqa_theorems_ndcg@10: 0.0237, bright_theoremqa_theorems_recall@100: 0.1338, bright_theoremqa_theorems_index_time_ms: 10036.9773, bright_theoremqa_theorems_query_time_ms: 709.9476, bright_economics_ndcg@10: 0.1692, bright_economics_recall@100: 0.4017, bright_economics_index_time_ms: 13690.0760, bright_economics_query_time_ms: 2064.8642, bright_biology_ndcg@10: 0.1851, bright_biology_recall@100: 0.4320, bright_biology_index_time_ms: 14821.0577, bright_biology_query_time_ms: 1967.3593, beir_fiqa_ndcg@10: 0.2365, beir_fiqa_recall@100: 0.5372, beir_fiqa_index_time_ms: 32658.4637, beir_fiqa_query_time_ms: 5428.9897, bright_earth_science_ndcg@10: 0.3091, bright_earth_science_recall@100: 0.6113, bright_earth_science_index_time_ms: 30082.7041, bright_earth_science_query_time_ms: 3260.2260, bright_stackoverflow_ndcg@10: 0.1834, bright_stackoverflow_recall@100: 0.4699, bright_stackoverflow_index_time_ms: 95736.5365, bright_stackoverflow_query_time_ms: 6836.8470, beir_trec-covid_ndcg@10: 0.6690, beir_trec-covid_recall@100: 0.1089, beir_trec-covid_index_time_ms: 128271.6072, beir_trec-covid_query_time_ms: 1277.5697, avg_ndcg@10: 0.2746, avg_recall@100: 0.4502, combined_score: 0.4151, total_index_time_ms: 359162.0078, total_query_time_ms: 42716.6912, total_time_ms: 401878.6991, datasets_evaluated: 12.0000, datasets_failed: 0.0000, error: 0.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  def tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.flo...\n      \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*nor...\n      return tf / (tf + k1 * norm + 1e-9)\n  \n  \n  def tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n      return tf / (tf + k1 * norm + 1e-9)\nwith:\n  def tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.flo...\n      \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*nor...\n      return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n  \n  \n  def tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n      return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\nChange 2: 'terms, weights = query_weights(query, self.k3, \"count\")' to 'terms, weights = query_weights(query, self.k3, \"saturated\")'\nChange 3: Replace:\n  class Parameters:\n      k1: float = 1.2\n      b: float = 0.75\n      k3: float = 8.0\nwith:\n  class Parameters:\n      k1: float = 0.9\n      b: float = 0.4\n      k3: float = 8.0\n- Metrics: beir_nfcorpus_ndcg@10: 0.3201, beir_nfcorpus_recall@100: 0.2551, beir_nfcorpus_index_time_ms: 3472.8422, beir_nfcorpus_query_time_ms: 89.2330, beir_scifact_ndcg@10: 0.6766, beir_scifact_recall@100: 0.9287, beir_scifact_index_time_ms: 4684.8969, beir_scifact_query_time_ms: 321.7798, bright_pony_ndcg@10: 0.0777, bright_pony_recall@100: 0.2497, bright_pony_index_time_ms: 1320.7125, bright_pony_query_time_ms: 411.0185, beir_arguana_ndcg@10: 0.2951, beir_arguana_recall@100: 0.9265, beir_arguana_index_time_ms: 5636.3012, beir_arguana_query_time_ms: 17326.7471, bright_theoremqa_theorems_ndcg@10: 0.0237, bright_theoremqa_theorems_recall@100: 0.1338, bright_theoremqa_theorems_index_time_ms: 10497.1010, bright_theoremqa_theorems_query_time_ms: 734.1264, beir_scidocs_ndcg@10: 0.1497, beir_scidocs_recall@100: 0.3478, beir_scidocs_index_time_ms: 19156.1029, beir_scidocs_query_time_ms: 2976.8008, bright_biology_ndcg@10: 0.1851, bright_biology_recall@100: 0.4320, bright_biology_index_time_ms: 14496.6697, bright_biology_query_time_ms: 1986.1966, bright_economics_ndcg@10: 0.1692, bright_economics_recall@100: 0.4017, bright_economics_index_time_ms: 13730.0053, bright_economics_query_time_ms: 2027.6329, beir_fiqa_ndcg@10: 0.2365, beir_fiqa_recall@100: 0.5372, beir_fiqa_index_time_ms: 32787.7219, beir_fiqa_query_time_ms: 5426.0575, bright_earth_science_ndcg@10: 0.3091, bright_earth_science_recall@100: 0.6113, bright_earth_science_index_time_ms: 31373.0327, bright_earth_science_query_time_ms: 3382.3958, bright_stackoverflow_ndcg@10: 0.1834, bright_stackoverflow_recall@100: 0.4699, bright_stackoverflow_index_time_ms: 97118.4015, bright_stackoverflow_query_time_ms: 7190.2599, beir_trec-covid_ndcg@10: 0.6690, beir_trec-covid_recall@100: 0.1089, beir_trec-covid_index_time_ms: 130890.9039, beir_trec-covid_query_time_ms: 1250.6080, avg_ndcg@10: 0.2746, avg_recall@100: 0.4502, combined_score: 0.4151, total_index_time_ms: 365164.6918, total_query_time_ms: 43122.8563, total_time_ms: 408287.5481, datasets_evaluated: 12.0000, datasets_failed: 0.0000, error: 0.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  def idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n      \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df),...\n      return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\nwith:\n  def idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n      \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df),...\n      return np.log((N + 1e-9) / (df + 1e-9))\n- Metrics: beir_nfcorpus_ndcg@10: 0.3201, beir_nfcorpus_recall@100: 0.2549, beir_nfcorpus_index_time_ms: 3530.1394, beir_nfcorpus_query_time_ms: 91.2915, beir_scifact_ndcg@10: 0.6780, beir_scifact_recall@100: 0.9287, beir_scifact_index_time_ms: 4705.3291, beir_scifact_query_time_ms: 322.0968, bright_pony_ndcg@10: 0.0775, bright_pony_recall@100: 0.2503, bright_pony_index_time_ms: 1312.6000, bright_pony_query_time_ms: 409.3517, beir_arguana_ndcg@10: 0.2952, beir_arguana_recall@100: 0.9265, beir_arguana_index_time_ms: 5750.3143, beir_arguana_query_time_ms: 17377.5628, bright_theoremqa_theorems_ndcg@10: 0.0238, bright_theoremqa_theorems_recall@100: 0.1338, bright_theoremqa_theorems_index_time_ms: 10513.0994, bright_theoremqa_theorems_query_time_ms: 745.3974, beir_scidocs_ndcg@10: 0.1499, beir_scidocs_recall@100: 0.3478, beir_scidocs_index_time_ms: 19144.0276, beir_scidocs_query_time_ms: 2984.2455, bright_biology_ndcg@10: 0.1837, bright_biology_recall@100: 0.4320, bright_biology_index_time_ms: 14462.6966, bright_biology_query_time_ms: 1948.9368, bright_economics_ndcg@10: 0.1692, bright_economics_recall@100: 0.4017, bright_economics_index_time_ms: 13651.6722, bright_economics_query_time_ms: 1985.6075, beir_fiqa_ndcg@10: 0.2367, beir_fiqa_recall@100: 0.5372, beir_fiqa_index_time_ms: 32453.1184, beir_fiqa_query_time_ms: 5475.8803, bright_earth_science_ndcg@10: 0.3094, bright_earth_science_recall@100: 0.6113, bright_earth_science_index_time_ms: 31261.4822, bright_earth_science_query_time_ms: 3366.4838, bright_stackoverflow_ndcg@10: 0.1845, bright_stackoverflow_recall@100: 0.4699, bright_stackoverflow_index_time_ms: 96603.0642, bright_stackoverflow_query_time_ms: 7016.8855, beir_trec-covid_ndcg@10: 0.6691, beir_trec-covid_recall@100: 0.1089, beir_trec-covid_index_time_ms: 129549.7250, beir_trec-covid_query_time_ms: 1254.4965, avg_ndcg@10: 0.2748, avg_recall@100: 0.4503, combined_score: 0.4152, total_index_time_ms: 362937.2684, total_query_time_ms: 42978.2361, total_time_ms: 405915.5045, datasets_evaluated: 12.0000, datasets_failed: 0.0000, error: 0.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.4152)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N + 1e-9) / (df + 1e-9))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Performs well on beir_nfcorpus_ndcg@10 (0.3201), Performs well on beir_nfcorpus_recall@100 (0.2549), Performs well on beir_nfcorpus_index_time_ms (3530.1394), Performs well on beir_nfcorpus_query_time_ms (91.2915), Performs well on beir_scifact_ndcg@10 (0.6780), Performs well on beir_scifact_recall@100 (0.9287), Performs well on beir_scifact_index_time_ms (4705.3291), Performs well on beir_scifact_query_time_ms (322.0968), Performs well on bright_pony_ndcg@10 (0.0775), Performs well on bright_pony_recall@100 (0.2503), Performs well on bright_pony_index_time_ms (1312.6000), Performs well on bright_pony_query_time_ms (409.3517), Performs well on beir_arguana_ndcg@10 (0.2952), Performs well on beir_arguana_recall@100 (0.9265), Performs well on beir_arguana_index_time_ms (5750.3143), Performs well on beir_arguana_query_time_ms (17377.5628), Performs well on bright_theoremqa_theorems_ndcg@10 (0.0238), Performs well on bright_theoremqa_theorems_recall@100 (0.1338), Performs well on bright_theoremqa_theorems_index_time_ms (10513.0994), Performs well on bright_theoremqa_theorems_query_time_ms (745.3974), Performs well on beir_scidocs_ndcg@10 (0.1499), Performs well on beir_scidocs_recall@100 (0.3478), Performs well on beir_scidocs_index_time_ms (19144.0276), Performs well on beir_scidocs_query_time_ms (2984.2455), Performs well on bright_biology_ndcg@10 (0.1837), Performs well on bright_biology_recall@100 (0.4320), Performs well on bright_biology_index_time_ms (14462.6966), Performs well on bright_biology_query_time_ms (1948.9368), Performs well on bright_economics_ndcg@10 (0.1692), Performs well on bright_economics_recall@100 (0.4017), Performs well on bright_economics_index_time_ms (13651.6722), Performs well on bright_economics_query_time_ms (1985.6075), Performs well on beir_fiqa_ndcg@10 (0.2367), Performs well on beir_fiqa_recall@100 (0.5372), Performs well on beir_fiqa_index_time_ms (32453.1184), Performs well on beir_fiqa_query_time_ms (5475.8803), Performs well on bright_earth_science_ndcg@10 (0.3094), Performs well on bright_earth_science_recall@100 (0.6113), Performs well on bright_earth_science_index_time_ms (31261.4822), Performs well on bright_earth_science_query_time_ms (3366.4838), Performs well on bright_stackoverflow_ndcg@10 (0.1845), Performs well on bright_stackoverflow_recall@100 (0.4699), Performs well on bright_stackoverflow_index_time_ms (96603.0642), Performs well on bright_stackoverflow_query_time_ms (7016.8855), Performs well on beir_trec-covid_ndcg@10 (0.6691), Performs well on beir_trec-covid_recall@100 (0.1089), Performs well on beir_trec-covid_index_time_ms (129549.7250), Performs well on beir_trec-covid_query_time_ms (1254.4965), Performs well on avg_ndcg@10 (0.2748), Performs well on avg_recall@100 (0.4503), Performs well on combined_score (0.4152), Performs well on total_index_time_ms (362937.2684), Performs well on total_query_time_ms (42978.2361), Performs well on total_time_ms (405915.5045), Performs well on datasets_evaluated (12.0000), Performs well on datasets_failed (0.0000), Performs well on error (0.0000)\n\n### Program 2 (Score: 0.4151)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Performs well on beir_nfcorpus_ndcg@10 (0.3201), Performs well on beir_nfcorpus_recall@100 (0.2551), Performs well on beir_nfcorpus_index_time_ms (3472.8422), Performs well on beir_nfcorpus_query_time_ms (89.2330), Performs well on beir_scifact_ndcg@10 (0.6766), Performs well on beir_scifact_recall@100 (0.9287), Performs well on beir_scifact_index_time_ms (4684.8969), Performs well on beir_scifact_query_time_ms (321.7798), Performs well on bright_pony_ndcg@10 (0.0777), Performs well on bright_pony_recall@100 (0.2497), Performs well on bright_pony_index_time_ms (1320.7125), Performs well on bright_pony_query_time_ms (411.0185), Performs well on beir_arguana_ndcg@10 (0.2951), Performs well on beir_arguana_recall@100 (0.9265), Performs well on beir_arguana_index_time_ms (5636.3012), Performs well on beir_arguana_query_time_ms (17326.7471), Performs well on bright_theoremqa_theorems_ndcg@10 (0.0237), Performs well on bright_theoremqa_theorems_recall@100 (0.1338), Performs well on bright_theoremqa_theorems_index_time_ms (10497.1010), Performs well on bright_theoremqa_theorems_query_time_ms (734.1264), Performs well on beir_scidocs_ndcg@10 (0.1497), Performs well on beir_scidocs_recall@100 (0.3478), Performs well on beir_scidocs_index_time_ms (19156.1029), Performs well on beir_scidocs_query_time_ms (2976.8008), Performs well on bright_biology_ndcg@10 (0.1851), Performs well on bright_biology_recall@100 (0.4320), Performs well on bright_biology_index_time_ms (14496.6697), Performs well on bright_biology_query_time_ms (1986.1966), Performs well on bright_economics_ndcg@10 (0.1692), Performs well on bright_economics_recall@100 (0.4017), Performs well on bright_economics_index_time_ms (13730.0053), Performs well on bright_economics_query_time_ms (2027.6329), Performs well on beir_fiqa_ndcg@10 (0.2365), Performs well on beir_fiqa_recall@100 (0.5372), Performs well on beir_fiqa_index_time_ms (32787.7219), Performs well on beir_fiqa_query_time_ms (5426.0575), Performs well on bright_earth_science_ndcg@10 (0.3091), Performs well on bright_earth_science_recall@100 (0.6113), Performs well on bright_earth_science_index_time_ms (31373.0327), Performs well on bright_earth_science_query_time_ms (3382.3958), Performs well on bright_stackoverflow_ndcg@10 (0.1834), Performs well on bright_stackoverflow_recall@100 (0.4699), Performs well on bright_stackoverflow_index_time_ms (97118.4015), Performs well on bright_stackoverflow_query_time_ms (7190.2599), Performs well on beir_trec-covid_ndcg@10 (0.6690), Performs well on beir_trec-covid_recall@100 (0.1089), Performs well on beir_trec-covid_index_time_ms (130890.9039), Performs well on beir_trec-covid_query_time_ms (1250.6080), Performs well on avg_ndcg@10 (0.2746), Performs well on avg_recall@100 (0.4502), Performs well on combined_score (0.4151), Performs well on total_index_time_ms (365164.6918), Performs well on total_query_time_ms (43122.8563), Performs well on total_time_ms (408287.5481), Performs well on datasets_evaluated (12.0000), Performs well on datasets_failed (0.0000), Performs well on error (0.0000)\n\n### Program 3 (Score: 0.4151)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Performs well on beir_nfcorpus_ndcg@10 (0.3201), Performs well on beir_nfcorpus_recall@100 (0.2551), Performs well on beir_nfcorpus_index_time_ms (3477.4060), Performs well on beir_nfcorpus_query_time_ms (89.8102), Performs well on beir_scifact_ndcg@10 (0.6766), Performs well on beir_scifact_recall@100 (0.9287), Performs well on beir_scifact_index_time_ms (4665.6751), Performs well on beir_scifact_query_time_ms (321.7332), Performs well on beir_arguana_ndcg@10 (0.2951), Performs well on beir_arguana_recall@100 (0.9265), Performs well on beir_arguana_index_time_ms (5682.0987), Performs well on beir_arguana_query_time_ms (17352.5388), Performs well on bright_pony_ndcg@10 (0.0777), Performs well on bright_pony_recall@100 (0.2497), Performs well on bright_pony_index_time_ms (1322.8384), Performs well on bright_pony_query_time_ms (406.5820), Performs well on beir_scidocs_ndcg@10 (0.1497), Performs well on beir_scidocs_recall@100 (0.3478), Performs well on beir_scidocs_index_time_ms (18716.5670), Performs well on beir_scidocs_query_time_ms (3000.2234), Performs well on bright_theoremqa_theorems_ndcg@10 (0.0237), Performs well on bright_theoremqa_theorems_recall@100 (0.1338), Performs well on bright_theoremqa_theorems_index_time_ms (10036.9773), Performs well on bright_theoremqa_theorems_query_time_ms (709.9476), Performs well on bright_economics_ndcg@10 (0.1692), Performs well on bright_economics_recall@100 (0.4017), Performs well on bright_economics_index_time_ms (13690.0760), Performs well on bright_economics_query_time_ms (2064.8642), Performs well on bright_biology_ndcg@10 (0.1851), Performs well on bright_biology_recall@100 (0.4320), Performs well on bright_biology_index_time_ms (14821.0577), Performs well on bright_biology_query_time_ms (1967.3593), Performs well on beir_fiqa_ndcg@10 (0.2365), Performs well on beir_fiqa_recall@100 (0.5372), Performs well on beir_fiqa_index_time_ms (32658.4637), Performs well on beir_fiqa_query_time_ms (5428.9897), Performs well on bright_earth_science_ndcg@10 (0.3091), Performs well on bright_earth_science_recall@100 (0.6113), Performs well on bright_earth_science_index_time_ms (30082.7041), Performs well on bright_earth_science_query_time_ms (3260.2260), Performs well on bright_stackoverflow_ndcg@10 (0.1834), Performs well on bright_stackoverflow_recall@100 (0.4699), Performs well on bright_stackoverflow_index_time_ms (95736.5365), Performs well on bright_stackoverflow_query_time_ms (6836.8470), Performs well on beir_trec-covid_ndcg@10 (0.6690), Performs well on beir_trec-covid_recall@100 (0.1089), Performs well on beir_trec-covid_index_time_ms (128271.6072), Performs well on beir_trec-covid_query_time_ms (1277.5697), Performs well on avg_ndcg@10 (0.2746), Performs well on avg_recall@100 (0.4502), Performs well on combined_score (0.4151), Performs well on total_index_time_ms (359162.0078), Performs well on total_query_time_ms (42716.6912), Performs well on total_time_ms (401878.6991), Performs well on datasets_evaluated (12.0000), Performs well on datasets_failed (0.0000), Performs well on error (0.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.3995)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 1.2\n    b: float = 0.6\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Alternative approach to beir_nfcorpus_ndcg@10, Alternative approach to beir_nfcorpus_recall@100\n\n### Program D2 (Score: 0.3996)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 1.1\n    b: float = 0.6\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N + 1.0) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nKey features: Alternative approach to beir_nfcorpus_ndcg@10, Alternative approach to beir_nfcorpus_recall@100\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.4151, Type: Migrant)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.3995, Type: Exploratory)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 1.2\n    b: float = 0.6\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N - df + 0.5) / (df + 0.5) + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.4151, Type: Experimental)\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"\nConstrained BM25 seed \u2014 safe search over known primitives.\n\nStay within BM25: tune hyperparameters (k1, b, k3), swap IDF/TF/length-norm\nformulas for known alternatives, and combine them meaningfully. No exploration\nof novel retrieval ideas; efficient grid-search over a known search space.\n\nEvaluator contract: BM25, Corpus, tokenize, LuceneTokenizer; BM25.rank(), BM25.score().\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom collections import Counter\nfrom concurrent.futures import ThreadPoolExecutor\nfrom typing import TYPE_CHECKING\n\nimport numpy as np\nfrom scipy.sparse import csr_matrix, lil_matrix\n\nfrom ranking_evolved.bm25 import LuceneTokenizer as _BaseLuceneTokenizer\n\nif TYPE_CHECKING:\n    from numpy.typing import NDArray\n\nNUM_QUERY_WORKERS = min(int(os.environ.get(\"BM25_QUERY_WORKERS\", 32)), 32)\nMIN_QUERIES_FOR_PARALLEL = 10\n\n\n# -----------------------------------------------------------------------------\n# Parameters \u2014 EVOLVE: k1, b, k3 (e.g. k1 in [0.5, 2.0], b in [0, 1])\n# -----------------------------------------------------------------------------\n\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n\n\n# -----------------------------------------------------------------------------\n# IDF \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, ATIRE, BM25L, BM25+)\n# -----------------------------------------------------------------------------\n\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N + 1e-9) / (df + 1e-9))\n\n\n# -----------------------------------------------------------------------------\n# TF saturation \u2014 EVOLVE: swap for known formulas (Lucene, Robertson, log, etc.)\n# -----------------------------------------------------------------------------\n\ndef tf_saturated(tf: NDArray[np.float64], k1: float, norm: NDArray[np.float64]) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene tf/(tf+k1*norm); Robertson (k1+1)*tf/(tf+k1*norm); log log(1+tf)/(tf+k1*norm).\"\"\"\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\ndef tf_saturated_scalar(tf: float, k1: float, norm: float) -> float:\n    return (k1 + 1.0) * tf / (tf + k1 * norm + 1e-9)\n\n\n# -----------------------------------------------------------------------------\n# Length norm \u2014 EVOLVE: swap for known formulas (pivoted, none, log, sqrt)\n# -----------------------------------------------------------------------------\n\ndef length_norm(doc_lengths: NDArray[np.float64], avgdl: float, b: float) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Pivoted 1-b+b*dl/avgdl; none 1.0; log 1/log(e+dl); sqrt 1/sqrt(dl).\"\"\"\n    return 1.0 - b + b * (doc_lengths / max(avgdl, 1.0))\n\n\n# -----------------------------------------------------------------------------\n# Query term weights \u2014 EVOLVE: unique (1 per term), count (qtf), saturated (k3)\n# -----------------------------------------------------------------------------\n\ndef query_weights(\n    query: list[str], k3: float, mode: str = \"count\"\n) -> tuple[list[str], NDArray[np.float64]]:\n    \"\"\"EVOLVE: unique (bag-of-words), count (qtf), saturated (k3+1)*qtf/(k3+qtf). Default count matches Pyserini.\"\"\"\n    if not query:\n        return [], np.array([], dtype=np.float64)\n    cnt = Counter(query)\n    terms = list(cnt.keys())\n    if mode == \"unique\":\n        w = np.ones(len(terms), dtype=np.float64)\n    elif mode == \"count\":\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    elif mode == \"saturated\":\n        qtf = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n        w = (k3 + 1.0) * qtf / (k3 + qtf)\n    else:\n        w = np.array([float(cnt[t]) for t in terms], dtype=np.float64)\n    return terms, w\n\n\n# -----------------------------------------------------------------------------\n# Tokenization (fixed for evaluator)\n# -----------------------------------------------------------------------------\n\n_TOKENIZER: _BaseLuceneTokenizer | None = None\n\ndef _get_tokenizer() -> _BaseLuceneTokenizer:\n    global _TOKENIZER\n    if _TOKENIZER is None:\n        _TOKENIZER = _BaseLuceneTokenizer()\n    return _TOKENIZER\n\ndef tokenize(text: str) -> list[str]:\n    return _get_tokenizer()(text)\n\nclass LuceneTokenizer:\n    def __init__(self):\n        self._tokenizer = _BaseLuceneTokenizer()\n    def __call__(self, text: str) -> list[str]:\n        return self._tokenizer(text)\n\n\n# -----------------------------------------------------------------------------\n# Corpus (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass Corpus:\n    def __init__(self, documents: list[list[str]], ids: list[str] | None = None):\n        self.documents = documents\n        self.ids = ids or [str(i) for i in range(len(documents))]\n        self._id_to_idx = {doc_id: i for i, doc_id in enumerate(self.ids)}\n        self.N = len(documents)\n        self.doc_lengths = np.array([len(d) for d in documents], dtype=np.float64)\n        self.avgdl = float(np.mean(self.doc_lengths)) if self.N > 0 else 1.0\n\n        self._vocab: dict[str, int] = {}\n        for doc in documents:\n            for term in doc:\n                if term not in self._vocab:\n                    self._vocab[term] = len(self._vocab)\n        V = len(self._vocab)\n\n        tf_lil = lil_matrix((V, self.N), dtype=np.float64)\n        self._posting_lists: dict[int, list[int]] = {i: [] for i in range(V)}\n        self._df = np.zeros(V, dtype=np.float64)\n\n        for doc_idx, doc in enumerate(documents):\n            seen = set()\n            for term, count in Counter(doc).items():\n                tid = self._vocab[term]\n                tf_lil[tid, doc_idx] = count\n                if tid not in seen:\n                    self._posting_lists[tid].append(doc_idx)\n                    self._df[tid] += 1\n                    seen.add(tid)\n\n        self.tf_matrix = csr_matrix(tf_lil)\n        self._posting_lists = {\n            tid: np.array(doc_ids, dtype=np.int64)\n            for tid, doc_ids in self._posting_lists.items()\n            if doc_ids\n        }\n\n    def __len__(self) -> int:\n        return self.N\n\n    def get_df(self, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self._df[tid]) if tid is not None else 0\n\n    def get_df_by_id(self, term_id: int) -> int:\n        return int(self._df[term_id])\n\n    def get_tf(self, doc_idx: int, term: str) -> int:\n        tid = self._vocab.get(term)\n        return int(self.tf_matrix[tid, doc_idx]) if tid is not None else 0\n\n    def get_tf_by_id(self, term_id: int, doc_idx: int) -> float:\n        return float(self.tf_matrix[term_id, doc_idx])\n\n    def get_posting_list(self, term: str) -> NDArray[np.int64]:\n        tid = self._vocab.get(term)\n        return self._posting_lists.get(tid, np.array([], dtype=np.int64)) if tid is not None else np.array([], dtype=np.int64)\n\n    def get_posting_list_by_id(self, term_id: int) -> NDArray[np.int64]:\n        return self._posting_lists.get(term_id, np.array([], dtype=np.int64))\n\n    def get_term_id(self, term: str) -> int | None:\n        return self._vocab.get(term)\n\n    def id_to_idx(self, ids: list[str]) -> list[int]:\n        return [self._id_to_idx[i] for i in ids if i in self._id_to_idx]\n\n    @property\n    def map_id_to_idx(self) -> dict[str, int]:\n        return self._id_to_idx\n\n    @property\n    def vocabulary_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def vocab_size(self) -> int:\n        return len(self._vocab)\n\n    @property\n    def idf_array(self) -> NDArray[np.float64]:\n        return idf(self._df, self.N)\n\n    @property\n    def term_doc_matrix(self) -> None:\n        return None\n\n\n# -----------------------------------------------------------------------------\n# BM25 (interface fixed for evaluator)\n# -----------------------------------------------------------------------------\n\nclass BM25:\n    def __init__(\n        self,\n        corpus: Corpus,\n        k1: float | None = None,\n        b: float | None = None,\n        k3: float | None = None,\n    ):\n        self.corpus = corpus\n        self.k1 = k1 if k1 is not None else Parameters.k1\n        self.b = b if b is not None else Parameters.b\n        self.k3 = k3 if k3 is not None else Parameters.k3\n        self.idf_array = idf(corpus._df, corpus.N)\n        self.norm_array = length_norm(corpus.doc_lengths, corpus.avgdl, self.b)\n        self._idf_by_term = {term: float(self.idf_array[tid]) for term, tid in corpus._vocab.items()}\n\n    def score_document(self, query_terms: list[str], doc_idx: int) -> float:\n        \"\"\"EVOLVE: same formula as vectorized path (IDF \u00d7 saturated TF, sum).\"\"\"\n        norm = self.norm_array[doc_idx]\n        s = 0.0\n        for term in query_terms:\n            idf_val = self._idf_by_term.get(term, 0.0)\n            if idf_val == 0:\n                continue\n            tf_val = self.corpus.get_tf(doc_idx, term)\n            if tf_val == 0:\n                continue\n            s += idf_val * tf_saturated_scalar(float(tf_val), self.k1, norm)\n        return s\n\n    def _score_candidates_vectorized(\n        self,\n        query_term_ids: list[int],\n        candidate_docs: NDArray[np.int64],\n        query_term_weights: NDArray[np.float64] | None = None,\n    ) -> NDArray[np.float64]:\n        if len(candidate_docs) == 0:\n            return np.array([], dtype=np.float64)\n        norms = self.norm_array[candidate_docs]\n        scores = np.zeros(len(candidate_docs), dtype=np.float64)\n        for i, tid in enumerate(query_term_ids):\n            idf_val = self.idf_array[tid]\n            if idf_val <= 0:\n                continue\n            w = query_term_weights[i] if query_term_weights is not None else 1.0\n            tf_row = self.corpus.tf_matrix[tid, candidate_docs].toarray().flatten()\n            scores += w * idf_val * tf_saturated(tf_row, self.k1, norms)\n        return scores\n\n    def rank(\n        self,\n        query: list[str],\n        top_k: int | None = None,\n    ) -> tuple[NDArray[np.int64], NDArray[np.float64]]:\n        if not query:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        terms, weights = query_weights(query, self.k3, \"saturated\")\n        term_ids = []\n        w_arr = []\n        for t, w in zip(terms, weights):\n            tid = self.corpus.get_term_id(t)\n            if tid is not None:\n                term_ids.append(tid)\n                w_arr.append(w)\n        if not term_ids:\n            return np.arange(self.corpus.N, dtype=np.int64), np.zeros(self.corpus.N, dtype=np.float64)\n        w_arr = np.array(w_arr, dtype=np.float64)\n        candidate_set: set[int] = set()\n        for tid in term_ids:\n            candidate_set.update(self.corpus.get_posting_list_by_id(tid).tolist())\n        candidate_docs = np.array(sorted(candidate_set), dtype=np.int64)\n        cand_scores = self._score_candidates_vectorized(term_ids, candidate_docs, w_arr)\n        all_scores = np.zeros(self.corpus.N, dtype=np.float64)\n        all_scores[candidate_docs] = cand_scores\n        sorted_indices = np.argsort(-all_scores).astype(np.int64)\n        sorted_scores = all_scores[sorted_indices]\n        if top_k is not None:\n            sorted_indices, sorted_scores = sorted_indices[:top_k], sorted_scores[:top_k]\n        return sorted_indices, sorted_scores\n\n    def batch_rank(\n        self,\n        queries: list[list[str]],\n        top_k: int | None = None,\n    ) -> list[tuple[NDArray[np.int64], NDArray[np.float64]]]:\n        if len(queries) < MIN_QUERIES_FOR_PARALLEL:\n            return [self.rank(q, top_k) for q in queries]\n        with ThreadPoolExecutor(max_workers=NUM_QUERY_WORKERS) as ex:\n            return list(ex.map(lambda q: self.rank(q, top_k), queries))\n\n    def score(self, query: list[str], doc_idx: int) -> float:\n        return self.score_document(query, doc_idx)\n\n\n__all__ = [\n    \"BM25\",\n    \"Corpus\",\n    \"tokenize\",\n    \"LuceneTokenizer\",\n    \"Parameters\",\n    \"idf\",\n    \"tf_saturated\",\n    \"length_norm\",\n    \"query_weights\",\n]\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["1) **Try a more standard BM25 IDF (Lucene-style)**. Your current `log(N/df)` can overweight ultra-rare terms and sometimes hurts top-10 ranking stability. Lucene\u2019s `log(1 + (N-df+0.5)/(df+0.5))` is a very common safe alternative and already performed almost identically in your history\u2014worth re-checking now with the current full configuration.\n\n<<<<<<< SEARCH\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log((N + 1e-9) / (df + 1e-9))\n=======\ndef idf(df: NDArray[np.float64], N: int) -> NDArray[np.float64]:\n    \"\"\"EVOLVE: Lucene default; alternatives: Robertson log((N-df+0.5)/(df+0.5)), ATIRE log(N/df), BM25L log((N+1)/(df+0.5)), BM25+ log((N+1)/df).\"\"\"\n    return np.log(1.0 + (N - df + 0.5) / (df + 0.5))\n>>>>>>> REPLACE\n\n\n2) **Slightly increase length normalization (b)**. Several weaker datasets here are \u201clong-document / verbose text\u201d heavy (e.g., StackOverflow, some BRIGHT domains). With `b=0.4`, long documents can be under-penalized; nudging upward is a classic BM25 tuning move that often improves nDCG@10 without killing recall.\n\n<<<<<<< SEARCH\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.4\n    k3: float = 8.0\n=======\nclass Parameters:\n    k1: float = 0.9\n    b: float = 0.55\n    k3: float = 8.0\n>>>>>>> REPLACE\n\n**Why these are \u201csafe\u201d**: both are standard BM25 choices (no structural changes), should not affect indexing/query time materially, and explore a nearby, well-known parameter/formula neighborhood that often trades a bit of recall for better early precision (which helps your 0.2\u00d7nDCG@10 term)."]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}